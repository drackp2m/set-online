mutation JoinGame($input: JoinGameInput!) {
  joinGame(input: $input) {
    uuid
    tableCards
    participants {
      uuid
      username
    }
    status
    expiresOn
    createdAt
    updatedAt
	}
}
